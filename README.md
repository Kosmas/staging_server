# Staging Server status application

## Installation

* Clone the application:

  git clone git@github.com:Kosmas/staging_server.git

* Rund bundle install

* Run migrations

  rake db:migrate

* Populate the database with seed records

  rake db:seed

* Run existing tests

  cucumber
  rake spec
